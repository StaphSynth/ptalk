<div class="posts-container">
  <ul class="post-list">
    <% @posts.each do |post| %>
      <li><%= render 'post', { post: post, current_user: @user } %></li>
    <% end %>
  </ul>

  <div class="post-form">
    <%= render 'form', { current_user: @user } %>
  </div>
</div>
